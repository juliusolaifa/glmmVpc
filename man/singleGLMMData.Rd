% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generation.R
\name{singleGLMMData}
\alias{singleGLMMData}
\title{Generate GLMM Data for a Single Model}
\usage{
singleGLMMData(
  beta,
  ns,
  Sigma = NULL,
  X = NULL,
  family = "gaussian",
  link = "identity",
  ...
)
}
\arguments{
\item{beta}{A vector of fixed effect coefficients.}

\item{ns}{A vector of sample sizes for each group.}

\item{Sigma}{A covariance matrix for the random effects.}

\item{X}{A matrix of predictors (design matrix) for the fixed effects.}

\item{family}{A string specifying the distribution family (default is "gaussian").
Supported families include "gaussian", "poisson", "negative_binomial", "tweedie",
"gamma", and "binomial".}

\item{link}{A string specifying the link function (default is "identity").}

\item{...}{Additional arguments to be passed to the family-specific data generation functions.}
}
\value{
A matrix where rows represent the predictors and the response variable (named "Feature").
}
\description{
This function generates data for a single generalized linear mixed model (GLMM).
It computes the mean of the model, generates response data using the specified
distribution family, and returns the data along with the design matrix.
}
\examples{
X <- matrix(1:30)
beta <- c(0.5, 0.2)
Sigma <- matrix(c(1, 0.5, 0.5, 1), 2, 2)
ns <- c(10, 10, 10)
singleGLMMData(beta, ns, Sigma, X, family = "gaussian", link = "identity")

}
