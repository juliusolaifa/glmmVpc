% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_matrices.R
\name{generateRandomInterceptMatrix}
\alias{generateRandomInterceptMatrix}
\title{Generate Random Intercept Design Matrix}
\usage{
generateRandomInterceptMatrix(ns)
}
\arguments{
\item{ns}{A vector specifying the number of observations per group
(cluster sizes).}
}
\value{
A matrix where each column represents a group, and each row
corresponds to an observation. The entries are 1 if the
observation belongs to the corresponding group and 0 otherwise.
}
\description{
This function generates a design matrix for random intercepts, where each
group has its own random intercept, and all observations within a group
share the same random effect.
}
\examples{
ns <- c(3, 3)  # Two groups, each with 3 observations
Z <- generateRandomInterceptMatrix(ns)
print(Z)
}
