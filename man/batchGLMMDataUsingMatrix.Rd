% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_glmm.R
\name{batchGLMMDataUsingMatrix}
\alias{batchGLMMDataUsingMatrix}
\title{Generate GLMM Data for Multiple Parameter Sets in Parallel}
\usage{
batchGLMMDataUsingMatrix(
  paramMat,
  ns,
  X = NULL,
  family = "gaussian",
  link = "identity",
  iter = 1,
  parallel = TRUE,
  num_cores = NULL
)
}
\arguments{
\item{paramMat}{A matrix where each row represents a set of parameters for generating GLMM data.
Columns should include fixed effect coefficients (beta) and random effect covariances (Sigma).}

\item{ns}{A vector specifying the number of observations per group (cluster sizes).}

\item{X}{A matrix of predictors (design matrix) for the fixed effects.}

\item{family}{A string specifying the distribution family. Supported families include
"gaussian", "poisson", "negative_binomial", "tweedie", and "gamma".}

\item{link}{A string specifying the link function (default is "identity").}

\item{iter}{An integer specifying the number of features to generate per parameter set (default is 1).}

\item{parallel}{A logical value indicating whether to run the computation in parallel (default is TRUE).}

\item{num_cores}{The number of cores to use for parallel processing. If NULL, the function will use all
available cores minus one (default is NULL).}
}
\value{
A matrix where rows represent the predictors from \code{X} followed by the generated feature data.
If parallel processing is used, the data is generated in parallel across multiple cores.
}
\description{
This function generates generalized linear mixed model (GLMM) data for multiple parameter sets.
It allows parallel processing for faster computation by distributing the tasks across multiple cores.
}
