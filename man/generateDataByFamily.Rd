% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generation.R
\name{generateDataByFamily}
\alias{generateDataByFamily}
\title{Generate Data Based on Specified Distribution Family}
\usage{
generateDataByFamily(family, mu, ns, args)
}
\arguments{
\item{family}{A string specifying the distribution family. Supported values
are: "gaussian", "poisson", "negative_binomial", "tweedie", "gamma",
"binomial".}

\item{mu}{A numeric value representing the mean or rate
(depending on the family).}

\item{ns}{A vector of integers representing the number of samples
for each group.}

\item{args}{A list of additional arguments specific to each distribution
family. The required
arguments for each family are:
\itemize{
\item "gaussian": \code{sd} (standard deviation, optional, defaults to 1)
\item "negative_binomial": \code{theta} (shape parameter, required)
\item "tweedie": \code{p} (power parameter, required) and \code{phi}
(dispersion parameter, required)
\item "gamma": \code{shape} (shape parameter, required)
}}
}
\value{
A vector of randomly generated data based on the specified family
and parameters.
}
\description{
This function generates random data from a specified distribution family.
Supported families include Gaussian, Poisson, Negative Binomial, Tweedie,
Gamma, and Binomial.
}
\examples{
# Generate Gaussian data with mean 0 and standard deviation 2
generateDataByFamily("gaussian", mu = 0, ns = c(10, 10, 10),
args = list(sd = 2))

# Generate Poisson data with rate 5
generateDataByFamily("poisson", mu = 5, ns = c(10, 10, 10), args = list())

# Generate Negative Binomial data
generateDataByFamily("negative_binomial", mu = 10, ns = c(10, 10),
args = list(theta = 1.5))

}
