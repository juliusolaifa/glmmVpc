% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmmData.R
\name{batchGLMMData}
\alias{batchGLMMData}
\title{Generate Multiple Datasets from a Generalized Linear Mixed Model (GLMM)}
\usage{
batchGLMMData(num_feat, X, beta, Sigma, ns, family, ...)
}
\arguments{
\item{num_feat}{An integer specifying the number of datasets (features)
to generate.}

\item{X}{A numeric vector of fixed effect covariates.}

\item{beta}{A numeric vector of fixed effect coefficients. The length of
\code{beta} should match the length of \code{X}.}

\item{Sigma}{A numeric matrix representing the covariance matrix for the
random effects.}

\item{ns}{A numeric vector specifying the number of observations per group.}

\item{family}{A character string specifying the family of distributions to
use. Supported values are \code{"nb"} for negative binomial and \code{"tw"} for
Tweedie distribution.}

\item{...}{Additional arguments passed to the family-specific random number
generation functions. For \code{"nb"} (negative binomial), use \code{theta}. For \code{"tw"}
(Tweedie), use \code{phi} and \code{power}.}
}
\value{
A numeric matrix where each row represents a generated dataset and
each column corresponds to a group specified in \code{ns}. The first rows of the
matrix correspond to the original \code{X} vector.
}
\description{
This function generates multiple datasets from a Generalized Linear Mixed
Model (GLMM) using the specified fixed effects, random effects, and family
of distributions. The generated datasets are combined into a single matrix.
}
\examples{
ns <- c(10, 20)
X <- rgen01(ns)
beta <- c(0.5, 2)
Sigma <- matrix(c(1, 0.5, 0.5, 1), nrow=2)
batchGLMMData(5, X, beta, Sigma, ns, family = "nb", theta = 1.5)
batchGLMMData(5, X, beta, Sigma, ns, family = "tw", phi = 1.2, power = 1.5)
}
